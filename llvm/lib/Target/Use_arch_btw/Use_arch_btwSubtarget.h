#pragma once

#include "Use_arch_btw.h"
#include "Use_arch_btwFrameLowering.h"
#include "Use_arch_btwISelLowering.h"
#include "llvm/CodeGen/TargetSubtargetInfo.h"

#define GET_SUBTARGETINFO_HEADER
#include "Use_arch_btwGenSubtargetInfo.inc"

namespace llvm {

class Use_arch_btwSubtarget : public Use_arch_btwGenSubtargetInfo {
public:
  Use_arch_btwSubtarget(const Triple &TT, const std::string &CPU,
                        const std::string &FS, const TargetMachine &TM);

  /// ParseSubtargetFeatures - Parses features string setting specified
  /// subtarget options.  Definition of function is auto generated by tblgen.
  void ParseSubtargetFeatures(StringRef CPU, StringRef TuneCPU, StringRef FS);

  const Use_arch_btwTargetLowering *getTargetLowering() const override {
    USE_ARCH_BTW_DUMP_CYAN
    return &TLInfo;
  }

  const Use_arch_btwFrameLowering *getFrameLowering() const override {
    USE_ARCH_BTW_DUMP_CYAN
    return &FrameLowering;
  }

private:
  Use_arch_btwTargetLowering TLInfo;
  Use_arch_btwFrameLowering FrameLowering;
};

} // end namespace llvm
